# GroundTruth Architecture (C4 Model)

## Context Diagram (C4 Level 1)

```mermaid
graph TB
    User[User<br/>Internal Team Member]
    GroundTruth[GroundTruth<br/>RAG Assistant]
    Documents[(Document Store<br/>PDF, MD, DOCX)]
    LLM[LLM Provider<br/>OpenAI / Anthropic]

    User -->|Upload documents| GroundTruth
    User -->|Ask questions| GroundTruth
    GroundTruth -->|Retrieve context| Documents
    GroundTruth -->|Generate answers| LLM
```

## Container Diagram (C4 Level 2)

```mermaid
graph TB
    User[User]

    subgraph GroundTruth[System Boundary]
        Frontend[Next.js Frontend<br/>React, Tailwind<br/>Port 3000]
        API[FastAPI Backend<br/>Python, Pydantic<br/>Port 8000]

        subgraph Services[Internal Services]
            Ingestion[Ingestion Service<br/>Parse → Chunk → Embed]
            Retrieval[Retrieval Service<br/>Hybrid Search + Rerank]
            Generation[Generation Service<br/>Prompt + Citation]
        end

        Database[(PostgreSQL + pgvector<br/>Documents, Chunks, Embeddings)]
    end

    LLM[LLM API<br/>OpenAI / Anthropic]

    User -->|HTTP| Frontend
    Frontend -->|REST API| API
    API --> Ingestion
    API --> Retrieval
    API --> Generation
    Ingestion -->|Store| Database
    Retrieval -->|Query| Database
    Generation -->|Chat Completions| LLM
```

## Component Diagram (C4 Level 3) - Retrieval Service

```mermaid
graph TB
    subgraph RetrievalService[Retrieval Service]
        API[API Layer<br/>FastAPI Routes]
        Hybrid[Hybrid Search<br/>Vector + Keyword]
        Rerank[Cross-Encoder Reranker]
        Citation[Citation Assembly]
        Confidence[Confidence Scoring]

        API --> Hybrid
        Hybrid --> Rerank
        Rerank --> Citation
        Citation --> Confidence
    end

    DB[(PostgreSQL/pgvector)]
    Hybrid -->|Vector search| DB
    Hybrid -->|Full-text search| DB
```

## Data Flow - Query Processing

```mermaid
sequenceDiagram
    participant U as User
    participant F as Frontend
    participant A as API
    participant R as Retrieval
    participant G as Generation
    participant DB as Database
    participant LLM as LLM Provider

    U->>F: Submit query
    F->>A: POST /api/query
    A->>R: retrieve(query)

    par Vector Search
        R->>DB: Semantic similarity
    and Keyword Search
        R->>DB: Full-text search
    end

    R->>R: Reciprocal Rank Fusion
    R->>R: Cross-encoder rerank
    R->>R: Filter by threshold

    R->>A: Retrieved chunks + scores
    A->>G: generate(query, context)

    alt Confidence > threshold
        G->>LLM: Completion request
        LLM->>G: Generated answer
        G->>A: Answer + citations
        A->>F: Response with trace
        F->>U: Display answer + sources
    else Confidence < threshold
        G->>A: Refusal with explanation
        A->>F: Refusal response
        F->>U: Display refusal
    end
```

## Key Architectural Decisions

| Decision | Rationale |
|----------|-----------|
| **Hybrid Search** | Combines semantic similarity (vector) with exact matching (keyword) for better recall |
| **Reranking** | Initial retrieval casts wide net; reranking with cross-encoder improves precision |
| **Citation Assembly** | Every claim traceable to source chunks enables verification and debugging |
| **Refusal Logic** | Better to say "I don't know" than hallucinate; maintains user trust |
| **Service Boundaries** | Each pipeline stage isolated for independent testing and scaling |
| **pgvector** | Co-located vector and relational storage simplifies deployment |

## Technology Stack

| Layer | Technology |
|-------|------------|
| Frontend | Next.js 14, React, TypeScript, Tailwind CSS |
| Backend | FastAPI, Pydantic, SQLAlchemy |
| Database | PostgreSQL 16, pgvector extension |
| Embeddings | OpenAI text-embedding-3-small |
| LLM | OpenAI GPT-4o / GPT-4o-mini |
| Deployment | Docker Compose |

## Scalability Considerations

- **Horizontal**: Stateless API servers behind load balancer
- **Database**: Read replicas for retrieval queries
- **Caching**: Redis for embedding cache (future)
- **Async**: Celery for document processing (future)
