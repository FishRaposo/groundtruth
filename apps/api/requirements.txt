# GroundTruth's canonical install is declared in ``pyproject.toml``:
#
#   python -m pip install -e "apps/api[dev]"
#
# This compatibility file deliberately delegates to that same project metadata
# when invoked from ``apps/api``. Optional parser/model/provider extras are
# documented in pyproject and are not required for the offline demo.
.[dev]
